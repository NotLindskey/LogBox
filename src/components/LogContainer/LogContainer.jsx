import React from 'react';

import './LogContainer.css';

function logContainer() {
  return (
    <div className="log-container">
      <p>This is a log container!</p>
    </div>
  );
}

export default logContainer;
